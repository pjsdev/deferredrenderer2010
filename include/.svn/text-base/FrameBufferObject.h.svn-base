#ifndef FRAMEBUFFEROBJECT_H
#define FRAMEBUFFEROBJECT_H

#include "string"

class FrameBufferObject
{
public:
    FrameBufferObject(unsigned int width, unsigned int height, const std::string &name);
    ~FrameBufferObject();

    void bind();
    void unbind();
    void create();
    void checkStatus();
    void activateAllTargets();
    void activateTarget1();
    void activateTarget2();
    void activateTarget3();
    void activateTarget4();

//private:
    void createTexture(unsigned int &_id);
    unsigned int m_width;
    unsigned int m_height;
    std::string m_name;

    // framebuffer id
    unsigned int m_FBOid;

    //depth buffer id
    unsigned int m_Depth;
    unsigned int m_Color1;
    unsigned int m_Color2;
    unsigned int m_Color3;
    unsigned int m_Color4;

    //stencil buffer id
    //unsigned int m_SBid;
};

#endif // FRAMEBUFFEROBJECT_H
