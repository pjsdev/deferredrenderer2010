#ifndef POINTLIGHT_H
#define POINTLIGHT_H

#include <string>
#include "ngl/Vector.h"

class PointLight
{
public:
    PointLight();
    PointLight(const float &_x, const float &_y, const float &_z, const float &_radius, const ngl::Vector &_intensity);
    ~PointLight();

    void draw();
    void generateVAO();
//private:
    unsigned int m_vaoID;
    float m_radius;
    ngl::Vector m_pos;
    ngl::Vector m_intensity;
};

#endif // POINTLIGHT_H
